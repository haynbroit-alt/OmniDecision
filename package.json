{
  "name": "omnidecision",
  "version": "0.1.0",
  "description": "Automatic cloud cost finding generator",
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli/index.js",
    "test": "jest --testPathIgnorePatterns='integration'",
    "test:integration": "jest --testPathPattern='integration'",
    "test:coverage": "jest --coverage --testPathIgnorePatterns='integration'"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.600.0",
    "@aws-sdk/client-ec2": "^3.600.0"
  }
}
